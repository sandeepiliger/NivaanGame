node_modules/
package-lock.json
.DS_Store
*.log

# Capacitor: native platform folders and staged web assets are regenerated
# on demand (npm run android:sync) — capacitor.config.json is real source
# and IS committed. google-services.json and any keystore are secrets —
# never commit those; see DEPLOYMENT.md.
android/
ios/
www/
google-services.json
*.keystore
*.jks
tools/shots/

# Native launcher/splash source art, rendered from assets/icon.svg by
# tools/make-app-assets.mjs (npm run android:assets). Generated, not source —
# the mascot itself lives in that script.
resources/
